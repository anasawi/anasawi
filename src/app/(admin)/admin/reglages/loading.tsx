import { AdminLoading } from '@/components/admin/AdminLoading'

/* Squelette d'attente des Réglages (colonne large, comme l'écran). */
export default function Loading() {
  return <AdminLoading width="wide" />
}
